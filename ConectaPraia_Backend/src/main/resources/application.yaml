#Futuramente para Deploy, aprender um jeito que guarda essas informações sem prejudicar a segurança
server:
  address: 0.0.0.0
  port: 8080

spring:
  application:
    name: ConectaPraia

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:ConectaPraia_db}
    username: ${DB_USER:Qdop}
    password: ${DB_PASS:Qdop123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  api:
    security:
      token:
        secret: ${JWT_SECRET:secret-qdop}